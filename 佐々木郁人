import 'package:flutter/material.dart';

void main(){
    runApp(MyApp());
}

classn MyApp extends StatelessWidget{

    @override
    Widget build(BuildContext context){
        return MateriaApp(
            title: 'Flutter Demo',
            home: Text(
                'Hello,
            )
        )
    }
}
